help me create a prompt for an ai website builder. I'm designing a web page that will be used to track from two to four ai code cli conversations at once so there needs to be a grid pattern for the conversation windows that grows or shrinks based on the number of open conversations. There should be no more than two per row, so at most it will be a 2x2 grid. There should be a single prompt input at the bottom for me to enter prompts that will be sent to the individual ai coders with the option to send to all or one or more selected ai coders. We already have backend code to handle the discussion, we are just designing the look of the frontend now. Next to each ai coder conversation box should be a button for 'Esc', a button for up-arrow, a button for down-arrow, and an enter symbol button. These should be aligned vertically with each respective conversation box. At the top of the page should be room for the project name 'Orchestrator'. The web page should have a modern, clean look, with a React type look (or similar modern design). Let me know if you need further clarification.