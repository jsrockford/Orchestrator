# 🗺️ Project Code Index (Lightweight)
*A high-level map of the codebase. For deep details, read `CODE_BIBLE.md`.*

## File List
- **`src\controllers\claude_controller.py`**: The `claude_controller.py` file defines a `ClaudeController` class that extends `TmuxController` to provide a specialized controller for interacting with the Claude Code AI assistant, handling sess...
- **`src\controllers\codex_controller.py`**: **Purpose:** Provides a `CodexController` class that extends `TmuxController` to configure a tmux session specifically for the Codex CLI, using Codex-specific defaults while maintaining shared tmux...
- **`src\controllers\gemini_controller.py`**: See full Bible for details.
- **`src\controllers\qwen_controller.py`**: The `qwen_controller.py` file defines a `QwenController` class that extends the `TmuxController` class to provide Qwen-specific configurations for the shared TmuxController. It is designed to handl...
- **`src\controllers\session_backend.py`**: The `session_backend.py` file defines an abstraction layer for managing interactive AI CLI sessions across various backend implementations (e.g., tmux, expect, PTY), providing a consistent interfac...
- **`src\controllers\tmux_controller.py`**: See full Bible for details.
- **`src\orchestrator\context_manager.py`**: The `ContextManager` class in `src/orchestrator/context_manager.py` provides a lightweight mechanism to track conversation history, decisions, conflicts, and other relevant events during AI orchest...
- **`src\orchestrator\control_channel.py`**: The `control_channel.py` module manages a named pipe (FIFO) that allows external processes to send control commands to influence an active orchestration run, providing a lightweight interface for h...
- **`src\orchestrator\conversation_manager.py`**: See full Bible for details.
- **`src\orchestrator\message_router.py`**: The `MessageRouter` class in `message_router.py` facilitates message routing between orchestrated AI controllers managed via tmux sessions, ensuring participants receive relevant updates in their p...
- **`src\orchestrator\orchestrator.py`**: The `orchestrator.py` file defines the `DevelopmentTeamOrchestrator` class, which coordinates multiple AI controllers, managing their automation states, queuing commands, and facilitating discussio...
- **`src\orchestrator\validation.py`**: The `validation.py` module provides post-completion validation helpers for orchestrated discussions, ensuring that completed conversations contain evidence of testing and optionally executing autom...
- **`src\orchestrator\web_api.py`**: See full Bible for details.
- **`src\utils\auto_restart.py`**: The `auto_restart.py` module provides utilities for automatically restarting failed sessions with configurable policies, backoff strategies, and attempt limits to prevent infinite loops.
- **`src\utils\config_loader.py`**: See full Bible for details.
- **`src\utils\exceptions.py`**: Defines a structured hierarchy of custom exception classes for handling errors in AI controller operations, enabling clear error identification and targeted recovery strategies.
- **`src\utils\health_check.py`**: The `health_check.py` module provides utilities to monitor the health and responsiveness of sessions, particularly tmux sessions, through various configurable checks.
- **`src\utils\logger.py`**: The `logger.py` module provides logging utilities for the orchestration project, configuring console and file logging based on defaults from `config.yaml`.
- **`src\utils\output_parser.py`**: The `output_parser.py` module provides utilities for parsing and cleaning Claude Code output captured from tmux, including functions to strip ANSI escape codes, extract and clean responses, and val...
- **`src\utils\path_helpers.py`**: The `src/utils/path_helpers.py` module provides utility functions to resolve project and tmux worktree paths, prioritizing environment variables, configuration settings, and default repository-rela...
- **`src\utils\retry.py`**: The `retry.py` module provides utilities for implementing retry logic with exponential backoff to handle transient failures in Python functions.