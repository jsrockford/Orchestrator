# Multi-AI CLI Interaction POC Configuration
# Values based on empirical testing and observation
# Supports: Claude Code, Gemini CLI, Codex CLI, and Qwen CLI
#
# IMPORTANT: Startup timeouts should be generous to handle real-world variability:
# - Network latency for credential loading
# - System resource availability
# - First-time initialization vs. cached startup
# - Better to wait longer than fail prematurely

claude:
  # Startup timing
  startup_timeout: 20  # Total seconds to wait for Claude to start (allows for variability)
  trust_wait: 3        # Wait after confirming trust prompt
  init_wait: 3         # Wait after initialization
  pane_width: 200      # tmux pane width in columns
  pane_height: 50      # tmux pane height in rows

  # Response timing
  response_timeout: 500     # Max seconds to wait for command response
  ready_check_interval: 0.5  # Seconds between output stability checks
  ready_stable_checks: 3     # Consecutive stable checks required
  debug_wait_logging: false  # Emit detailed wait_for_ready diagnostics
  ready_stabilization_delay: 1.0  # Pause after ready indicator before sending first command

  # Command patterns and indicators
  prompt_pattern: ">"                    # Input prompt (not reliable for ready detection)
  ready_indicators:                      # Patterns indicating ready state
    - "────────────────────────"         # Separator line
    - "? for shortcuts"                  # Status line
  loading_indicators:
    - "⎿"
    - "Running…"
    - "Waiting…"
    - "Working…"
    - "(esc to interrupt"
    - "esc to interrupt"
  thinking_indicator: "Lollygagging…"    # Shows when processing
  submit_key: "Enter"                    # Key tmux should send to submit commands
  text_enter_delay: 0.6                  # Delay between text and Enter

  # Path to Claude Code executable
  executable: "claude"  # Wrapper enforces sandboxed execution
  executable_args:
    - "--dangerously-skip-permissions"

  # Response marker
  response_marker: "●"  # Bullet point for Claude responses
  pause_on_manual_clients: false  # Allow read-only tmux attach without pausing automation

gemini:
  # Startup timing
  startup_timeout: 60  # Total seconds to wait for Gemini to start (allows for variability, credential loading, etc.)
  init_wait: 3         # Wait after initialization
  pane_width: 220      # Gemini often renders wide tables; give extra width
  pane_height: 50

  # Response timing
  response_timeout: 900      # Max seconds to wait for command response
  tool_timeout: 15          # Additional time for tool execution
  ready_check_interval: 0.5  # Seconds between output stability checks
  ready_stable_checks: 6     # Consecutive stable checks required
  debug_wait_logging: false  # Emit detailed wait_for_ready diagnostics
  ready_stabilization_delay: 2.0  # Gemini tends to need longer to accept first input

  # Command patterns and indicators
  prompt_pattern: ">"                    # Input prompt
  ready_indicators:                      # Patterns indicating ready state
    - "Type your message or @path/to/file"
    - "Model:"                              # Transcript prefix
  loading_indicators:                    # Shows when processing
    - "⠦"
    - "⠼"
    - "⠧"
    - "⠏"
    - "⠋"
    - "⠙"
    - "⠹"
    - "⠸"
    - "Enhancing..."
    - "Counting electrons..."
  loading_indicator_settle_time: 3.0
  response_complete_markers:
    - "› "
    - "Type your message or @path/to/file"
  submit_key: "C-m"                      # Gemini prefers Ctrl-M when running in tmux
  text_enter_delay: 0.5                  # Give Gemini extra time to ingest pasted text
  post_text_delay: 0.5                  # Pause after pasting before submitting

  # Response marker
  response_marker: "✦"  # Sparkle for Gemini responses
  pause_on_manual_clients: false  # Allow read-only tmux attach without pausing automation

  # Tool support
  supports_tools: true   # Gemini can use external tools
  tool_marker: "✓"       # Checkmark for successful tool execution

  # Path to Gemini executable
  executable: "gemini"  # Wrapper enforces sandboxed execution
  executable_args:
    - "--yolo"

codex:
  startup_timeout: 20
  init_wait: 1
  pane_width: 200
  pane_height: 50
  # Codex can take a while on longer completions; align with Claude's higher ceiling.
  response_timeout: 500
  ready_check_interval: 0.5
  ready_stable_checks: 4
  debug_wait_logging: false  # Emit detailed wait_for_ready diagnostics
  ready_stabilization_delay: 1.0
  prompt_pattern: ">"
  ready_indicators:
    - "OpenAI Codex"
    - "100% context left"
    - "Write tests for"
  loading_indicators:
    - "esc to interrupt)"
  loading_indicator_settle_time: 2.5
  response_complete_markers:
    - "Worked for"
    - "› "
  submit_key: "Enter"
  text_enter_delay: 0.1
  response_marker: "▸"
  executable: "codex"
  executable_args: []
  pause_on_manual_clients: false

qwen:
  # Startup timing
  startup_timeout: 25
  init_wait: 2
  pane_width: 200
  pane_height: 50

  # Response timing
  response_timeout: 900
  ready_check_interval: 0.5
  ready_stable_checks: 4
  debug_wait_logging: false

  # Command patterns and indicators
  ready_indicators:
    - "▸ Type your message or @path/to/file"
    - "Type your message or @path/to/file"
  loading_indicators:
    - "(esc to cancel"
    - "(escape to cancel"
  loading_indicator_settle_time: 1.5
  ready_stabilization_delay: 0.75
  submit_key: "Enter"
  text_enter_delay: 0.6
  post_text_delay: 0.0
  strip_ansi_for_indicators: true

  # Response marker and executable
  response_marker: "▸"
  executable: "qwen"
  executable_args:
    - "--yolo"
  pause_on_manual_clients: false
  interactive_command_guard:
    enabled: true
    timeout_seconds: 10
    require_spinner: true
    spinner_patterns:
      - "Figuring out how to make this more witty"
      - "⠸"
      - "⠴"
      - "⠼"
      - "⠧"
    allow_patterns:
      - "pytest"
      - "python -m pytest"
      - "python -m unittest"
      - "coverage run"
    cooldown_seconds: 4
    send_ctrl_c: true
    log_level: "warning"


human:
  # Human participant configuration
  turn_timeout: 300  # Seconds to wait for human response (5 minutes); 0 to disable
  allow_empty_submissions: false  # Reject blank human responses
  response_marker: "👤"  # Emoji displayed in transcripts for human turns

tmux:
  # Session configuration
  claude_session: "claude"   # Default Claude session name
  gemini_session: "gemini"   # Default Gemini session name
  codex_session: "codex"     # Default Codex session name
  qwen_session: "qwen"       # Default Qwen session name
  default_pane_width: 200
  default_pane_height: 50

  # Output capture settings
  capture_lines: 500     # Default lines to capture from pane
  capture_delay: 0.1     # Seconds to wait before capturing output

  # Command injection settings
  text_enter_delay: 0.1  # Delay between text and Enter key (seconds)

  # Session management
  kill_timeout: 2        # Seconds to wait after killing session

logging:
  # Logging configuration
  level: "DEBUG"          # DEBUG, INFO, WARNING, ERROR
  file: "/home/dgray/Projects/Orchestrator/logs/orchestrator.log"   # Log file path
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  max_bytes: 10485760    # 10MB
  backup_count: 3        # Number of backup files to keep

context_management:
  enabled: true
  clear_signal: "[[CLEAR]]"
  debounce_seconds: 30

  # Context validation - WHITELIST approach
  # Only honor [[CLEAR]] signals in these contexts
  valid_contexts:
    - "orchestrated_turn"    # During orchestrated discussions
    - "MessageBoard.md"      # When posted to MessageBoard

  # Supported agents and their clear commands
  agents:
    claude:
      clear_command: "/clear"
    gemini:
      clear_command: "/clear"
    qwen:
      clear_command: "/clear"
    codex:
      clear_command: "/new"  # Codex uses /new instead of /clear

  # Post-clear behavior
  post_clear_prompt: "Context cleared. Re-read PRD.md, ARCHITECTURE.md, and the next section of PROJECT_TASKS.md before continuing."
  required_rereads:
    - "PRD.md"
    - "ARCHITECTURE.md"
    - "PROJECT_TASKS.md"

  # Turn-count check-in (OPTIONAL - deferred to Phase 3)
  turn_count_checkin:
    enabled: false
    interval: 12
    prompt: "System Check: You have completed {turn_count} turns. Review your progress in PROJECT_TASKS.md. If this is a logical checkpoint, reply with [[CLEAR:{agent}]] to clear context. Otherwise reply with /continue to proceed."

  # Logging
  log_file: "logs/context_clears.log"
  log_level: "INFO"

response_validation:
  noise_log: "/home/dgray/Projects/Orchestrator/logs/filtered_patterns.log"
  error_log: "/home/dgray/Projects/Orchestrator/logs/response_errors.log"
  max_retries: 2
  retry_backoff_seconds:
    - 0.6
    - 1.2
  ignore_patterns:
    - "CLI update available"
    - "Attempting to automatically update"
    - "Installed with npm"
    - "npm install"
  error_patterns:
    - "Failed to login"
    - "Authentication failed"
    - "Invalid API key"
    - "Rate limit exceeded"
    - "Too many requests"
    - "Connection timeout"
    - "Network error"
    - "UNABLE_TO_GET_ISSUER_CERT"
    - "API error"
    - "API Error"
    - "Context length exceeded"
    - "Maximum tokens exceeded"
    - "FatalTurnLimitedError"
    - "MODULE_NOT_FOUND"
    - "Operation not permitted"
    - "Permission denied"
  min_response_length: 20
  require_response_marker: true

completion_detection:
  enabled: true
  debug_logging: true
  mode: "hybrid"  # Options: disabled, explicit, passive, hybrid
  explicit_signal: "[[PROJECT_COMPLETE]]"
  fallback_phrases:
    - "project is complete"
    - "project is now complete"
    - "all objectives have been met"
    - "no further actionable steps"
    - "successfully completed"
  cooldown_turns: 0            # Minimum turns before detection becomes active
  reset_on_disagreement: true  # Reset signals if an agent indicates more work is needed
  disagreement_phrases:
    - "not complete"
    - "more work needed"
    - "still need to"
    - "haven't finished"
    - "additional tasks remain"
  consensus:
    threshold: 0.66          # Percentage of participants who must agree
    recency_window: 2        # Only count signals from the last N turns
    require_consecutive: true  # Signals must appear in consecutive turns
  require_explicit_from_all: true  # All participants must send explicit signal before consensus

control_channel:
  enabled: true
  pipe_path: "/tmp/orchestrator_control"
  status:
    colorize: true
    file_colorize: false
    write_file: true
    file_path: "/tmp/orchestrator_status.txt"
    refresh_seconds: 5
    progress_bar_width: 24

loop_detection:
  enabled: true
  tool_loops:
    enabled: true
    repeat_threshold: 4          # Number of identical tool calls before flagging a loop
    history_window: 6            # How many recent turns to track per participant
    escalate_on_repeat: true     # Escalate if the next turn repeats the same loop
    ignore_tools: []             # Tool names to ignore (case-insensitive)

post_completion_validation:
  enabled: true
  require_test_mentions: true
  mention_keywords:
    - "test"
    - "tests"
    - "pytest"
    - "unit test"
    - "integration test"
  test_file_globs:
    - "test_*.py"
    - "tests/test_*.py"
  execute_tests: false
  test_command: "python -m pytest"
  command_timeout: 120

worktree:
  # Git worktree configuration for multi-instance testing
  main_path: "/home/dgray/Projects/Orchestrator"
  tmux_path: "/home/dgray/Projects/TestOrch"

test_commands:
  # Sample commands for testing
  simple:
    - "What is 2 + 2?"
    - "What is Python?"
    - "List 3 programming languages"

  context:
    - "Read file: spec.md"
    - "Summarize the main goals"
    - "What is the project about?"

  file_operations:
    - "List files in src/"
    - "Read src/controllers/tmux_controller.py"
    - "Explain the wait_for_ready method"

performance:
  # Performance measurement settings
  measure_latency: true
  log_timings: true

  # Expected performance (for validation)
  expected_command_latency_ms: 100
  expected_capture_latency_ms: 500
  expected_startup_time_s: 10

experimental:
  # Experimental features (not yet implemented)
  auto_retry_on_failure: false
  max_retries: 3
  streaming_capture: false
  parallel_sessions: false

interactive_macros:
  claude:
    status:
      command: "/status"
      description: "Show session status"
      category: "slash"
    compact:
      command: "/compact"
      description: "Summarize to free context"
      category: "slash"
    reset:
      command: "/reset"
      description: "Reset the conversation"
      category: "slash"
    cycle_permission_mode:
      keys: ["S-Tab"]
      description: "Cycle: Auto-Accept → Plan → Normal"
      category: "shift"

  gemini:
    chat:
      command: "/chat"
      description: "/chat (checkpoint menu)"
      category: "slash"
    chat_save:
      command: "/chat save <tag>"
      description: "Save checkpoint"
      category: "slash"
    chat_resume:
      command: "/chat resume <tag>"
      description: "Resume checkpoint"
      category: "slash"
    chat_list:
      command: "/chat list"
      description: "List checkpoints"
      category: "slash"
    mcp:
      command: "/mcp"
      description: "List MCP servers/tools"
      category: "slash"
    compress:
      command: "/compress"
      description: "Summarize to free context"
      category: "slash"
    toggle_yolo:
      keys: ["C-y"]
      description: "Toggle YOLO mode (auto-approve all)"
      category: "ctrl"

  codex:
    set_approvals:
      command: "/approvals"
      description: "Open approval policy picker"
      category: "slash"
    switch_model:
      command: "/model"
      description: "Switch active model"
      category: "slash"
    status:
      command: "/status"
      description: "Show session status"
      category: "slash"
    compact:
      command: "/compact"
      description: "Summarize to free context"
      category: "slash"

  qwen:
    toggle_yolo:
      keys: ["S-Tab"]
      description: "Toggle YOLO mode"
      category: "shift"
    mcp:
      command: "/mcp"
      description: "List MCP servers/tools"
      category: "slash"
    tools:
      command: "/tools"
      description: "List tools (desc/nodesc/schema)"
      category: "slash"
